# Historical adapters

Production code was migrated to `core`. This directory is retained only as a
historical marker and is not required at runtime. It intentionally contains no
executable Python code.
