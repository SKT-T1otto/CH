# algorithms

Shared MADDPG implementation and supporting agent, network, exploration-noise,
and tensor utility modules. The public package export is
`core.algorithms.MADDPG`. Chapter-specific BSER, RCAG, and VSGC algorithms do
not belong here unless a component becomes genuinely shared infrastructure.
