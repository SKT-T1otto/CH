# mapping

Implemented occupancy mapping and deterministic online A* path planning used by
the unknown-obstacle Chapter-3 mission profiles. Public implementation modules
are `core.mapping.map_module` and `core.mapping.path_planner`. CH4 robustness
logic is not implemented here.
