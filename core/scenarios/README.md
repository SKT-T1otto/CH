# scenarios

Canonical scenario schemas, identities, manifests, registry, and deterministic
E0 generator for profiles M00, M10, M20, and M90. Use
`core.scenarios.generator` for public generation; the migrated implementation
closure is internal to this package. Frozen E0 manifests must not be overwritten.
