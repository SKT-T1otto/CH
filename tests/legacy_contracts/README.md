# Legacy environment contracts

Frozen by Phase 0A.1 from read-only CPU smoke runs. Treat any fixture change as a semantic-review event. These fixtures are evidence/contract data only; they do not implement BSER, RCAG, or VSGC.
